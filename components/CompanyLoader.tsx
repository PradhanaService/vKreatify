"use client";

import type { Client } from "../data/clients";

type CompanyLoaderProps = {
  client: Client;
};

export default function CompanyLoader({ client: _ }: CompanyLoaderProps) {
  return null;
}
